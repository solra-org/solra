apiVersion: apps/v1
kind: Deployment
metadata:
  name: gitlab
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gitlab
  template:
    metadata:
      labels:
        app: gitlab
    spec:
      containers:
        - name: gitlab
          image: gitlab/gitlab-ce:latest
          ports:
            - containerPort: 8080
            - containerPort: 443
            - containerPort: 22
          env:
            - name: GITLAB_OMNIBUS_CONFIG
              value: |
                external_url 'http://10.5.100.21:30004'
                nginx['listen_port'] = 8080
                nginx['listen_https'] = false
                nginx['redirect_http_to_https'] = false
          volumeMounts:
            - mountPath: /etc/gitlab
              name: gitlab-config
            - mountPath: /var/log/gitlab
              name: gitlab-logs
            - mountPath: /var/opt/gitlab
              name: gitlab-data
      volumes:
        - name: gitlab-config
          hostPath:
            path: /mnt/gitlab/config
            type: DirectoryOrCreate
        - name: gitlab-logs
          hostPath:
            path: /mnt/gitlab/logs
            type: DirectoryOrCreate
        - name: gitlab-data
          hostPath:
            path: /mnt/gitlab/data
            type: DirectoryOrCreate

---
apiVersion: v1
kind: Service
metadata:
  name: gitlab
spec:
  selector:
    app: gitlab
  type: NodePort
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30004
    - name: https
      port: 443
      targetPort: 8443
      nodePort: 31004
    - name: ssh
      port: 22
      targetPort: 22
      nodePort: 30654