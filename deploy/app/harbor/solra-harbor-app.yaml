apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: solra-harbor-app
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/fisa4th-final-project/solra.git
    targetRevision: dev
    path: deploy/app/harbor
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: solra-harbor
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
